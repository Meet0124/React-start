import React from 'react';
import ReactDOM from 'react-dom/client';

import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
// reactdomis a virtual dom. two types of dom react and browser dom. it tries to create exact cop
root.render(
  
    <App />
  
);


